package s105;

public class MyList {
	Node head = null;
	Node cur;
	Node next;

	public void add(int value) {

		if (head == null) {
			head = new Node(value);
		}
		return;

		while (next != null) {

			cur.getNext();
			cur = next;

		}
	}
}